spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/testWaitingList
    username: mohammed
    password: superman
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8081